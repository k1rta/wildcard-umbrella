#!/usr/bin/env sh
echo "🔍 Running pre-push checks..."

# Only run type check (fast)
echo "📝 Running type check..."
npm run type-check || {
  echo "❌ Type check failed"
  exit 1
}

# Quick lint (already ran in pre-commit, but double check)
echo "📝 Running lint check..."
npm run lint || {
  echo "❌ Lint check failed"
  exit 1
}

# ✅ Run tests in CI only (not locally)
echo "⚠️  Skipping tests (will run in CI)"
echo "✅ All pre-push checks passed!"
